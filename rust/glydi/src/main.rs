//! The GLYDI binary: wiring only, config, headless mode. Placeholder.

// Placeholder: wiring will fail, so the signature is the real one.
#[allow(clippy::unnecessary_wraps)]
fn main() -> anyhow::Result<()> {
    Ok(())
}
